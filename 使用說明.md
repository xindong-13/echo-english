# Echo 英語 — 安裝與使用說明

一個只屬於你的英文句型與口說練習 App。完全免費、離線可用，手機與電腦進度自動同步。

---

## 一、先在 iPhone 上裝起來（約 5 分鐘）

網頁 App 需要一個網址才能安裝到主畫面。免費做法二選一，**Netlify Drop 最快**。

### 做法 A：Netlify Drop（不用寫程式，最推薦）

1. 電腦上打開 https://app.netlify.com/drop
2. 把整個 `echo` 資料夾**直接拖進網頁**
3. 幾秒後會給你一個網址，像 `https://xxx-yyy.netlify.app`
4. 註冊一個免費帳號（用 Google 登入即可），網址就永久有效，還能改成好記的名字
5. iPhone 用 **Safari** 打開那個網址 → 點下方「分享」→ 選「加入主畫面」

裝好後主畫面會出現一個綠色麥克風圖示，點開就是全螢幕 App，沒有網址列。

### 做法 B：GitHub Pages（想長期自己維護的話）

1. 註冊 GitHub → 建一個新的 repository（設為 Public）
2. 把 `echo` 資料夾裡的所有檔案上傳到根目錄
3. Settings → Pages → Source 選 `main` 分支 → Save
4. 等一兩分鐘，網址是 `https://你的帳號.github.io/repo名稱/`
5. 同樣用 Safari 打開 → 分享 → 加入主畫面

> **一定要用 Safari 加入主畫面**，Chrome 在 iOS 上不支援。
> 第一次打開後，App 會把自己快取起來，之後**沒有網路也能練習**（只有 AI 功能需要連網）。

---

## 二、四種練習模式

| 模式 | 每日目標 | 你看到什麼 | 練什麼 |
|---|---|---|---|
| **中翻英** | 5 題 | 只有中文 | 反應速度。最接近真實開口的壓力 |
| **聽力** | 5 題 | 什麼都不給，只播聲音 | 耳朵。聽到什麼就說什麼 |
| **填空** | 10 題 | 中文＋英文句挖掉關鍵字 | 單字拼寫與用法，不用出聲 |
| **重組句型** | 5 題 | 中文＋打散的單字 | 語序與句型結構，不用出聲，通勤時也能練 |

每個模式**各自**有每日目標，一天總共 25 題。練習首頁上方列出四條進度，點任一條就直接切到那個模式。單項達標會提示還剩哪些，四項全滿會慶祝一次。目標數字可在「我的 → 每日目標」自己調，設 0 就是不列入。

不管哪個模式，答案都要自己先想／先說出來，按「👁 看答案」才會顯示。

**填空模式**看中文提示，把英文句子空格裡的字打出來。想不出來可以按「💡 提示」逐字露出字母（每按一次扣 15 分），或直接「看答案」。「單字強化」那個分類的 80 句是專門為填空設計的——句子都是日常會說的，但故意放進 straightforward、reluctant、overwhelmed 這種你聽得懂卻說不出來的字。

練習中隨時可以按左上角「← 上一題」回去重做前面的題目。

說完之後會逐字比對評分：

- 🟩 **綠色** — 說對了
- 🟨 **黃色虛線** — 發音接近但不完全對（點著看會顯示辨識到什麼）
- 🟥 **紅色刪節線** — 漏掉沒說
- ⬜ **灰色斜體** — 多說出來的字

分數 85 以上算通過。**麥克風不能用的時候，隨時可以按「⌨️ 用打的」**，一樣會比對評分。

---

## 二之二、同一句不只一種說法

中翻英最常見的委屈是：**你講的其實也對，只是跟參考答案不一樣。**這件事這樣處理：

- **自動判斷**：中翻英分數不到 85 分時，AI 會自動看一次你那句。只要意思對、文法對、母語者聽得懂，就直接改判為正確（88 分以上），並告訴你你的說法跟參考答案語感差在哪
- **存起來，之後離線也算對**：AI 認可的說法會存進那一題。下次你再講同一種說法，不用連網、不用花錢，直接算對
- **手動觸發**：關掉自動判斷的話，結果頁會有「🤔 我這樣說也對嗎？」可以自己按
- **主動學更多**：按「📚 其他說法」，AI 會給你 3 種母語者也常用的講法，並標註哪句比較口語、哪句比較正式，全部自動存成可接受答案

例如「可以買單嗎？」標準答案是 `Could we get the check, please?`，你說 `Can we have the bill, please?` 一開始只有 41 分，AI 判定正確後存起來，之後這樣講就是 100 分。

存過的說法在「題庫」點進任何一句都看得到，也可以單獨刪掉。設定在「我的 → AI 設定 → 中翻英自動 AI 判斷」。

---

## 三、間隔複習（不用自己管）

每練一句，App 會依你的分數自動決定下次什麼時候再考你：

- 說得好 → 1 天 → 3 天 → 8 天 → 20 天…（間隔越拉越長）
- 說得差 → 打回原點，明天再來

所以每次按「開始練習」，系統會**優先塞給你該複習的句子**，剩下的位置才放新句。你只要每天打開來把四項目標填滿就好。

---

## 四、錯題本

**不用自己管，答錯的會自己進來。**

- 口說分數低於 70、填空答錯、句型排錯 → 自動收進錯題本，還會記下你當時錯在哪個字
- 練習首頁會出現紅色的「錯題本」卡片，按「複習錯題」就只練這些
- 同一句**連續兩次拿到 85 分以上**，自動移出錯題本
- 按「看清單」可以看全部錯句、聽發音，或手動移除某一句

---

## 五、AI 導師（選用，需要金鑰）

App 支援兩家 AI，在「我的 → AI 設定」上方一鍵切換。兩家的金鑰可以各自存著，隨時換。

| | Google Gemini | Anthropic Claude |
|---|---|---|
| 費用 | **完全免費** | 付費，需先儲值約 5 美金 |
| 信用卡 | **不用** | 要 |
| 用量限制 | 免費層每天有次數上限，Flash-Lite 最寬鬆 | 沒有上限，用多少付多少 |
| 英文教學品質 | 夠好 | 較細膩 |
| 資料隱私 | 免費層內容 Google 可能拿去改進產品 | 不會用於訓練 |

一天練十題加一次聊天檢討大概用 15 次，**免費額度綽綽有餘**，建議先用 Gemini。

> **Claude Pro／Max 訂閱不能拿來用。** 訂閱和 API 是兩套完全獨立的計費，訂閱不含 API 額度，這是 Anthropic 的官方政策。想用 Claude 就只能另外儲值。

### 拿 Gemini 免費金鑰（約 2 分鐘）

1. 手機或電腦開 **https://aistudio.google.com/apikey**
2. 用 Google 帳號登入 → 按 **Create API key**
3. 複製整串金鑰（**`AIza`** 或 **`AQ.`** 開頭都是對的）
4. App →「我的」→ **AI 設定** → 上面選 **Google Gemini（免費）** → 貼上金鑰 → 鍵盤按「完成」
5. 按 **測試連線**，看到「連線成功 ✅」就好了

如果哪天跳出「今天的免費額度用完了」，把模型換成 **Gemini 3.5 Flash-Lite**（免費額度最寬鬆），或等隔天重置。

**如果出現「模型不可用」**：Google 會不定期停掉舊模型給新帳號用。App 遇到這種錯誤會自動往下試備援模型並記住能用的那個，通常你不會察覺。若還是不行，按設定裡的「🔍 偵測哪些模型我能用」，它會一個個試打並自動選一個可用的。

### 拿 Anthropic 金鑰（想要更好的教學品質時）

1. 開 **https://console.anthropic.com** 註冊登入
2. 左側 **API keys** → **Create Key** → **立刻複製 `sk-ant-` 開頭那串**（關掉就看不到了）
3. 左側 **Billing** → **Add credits** 儲值。**沒儲值金鑰會一直顯示無效**，這是最多人卡住的地方
4. App 裡選 **Anthropic Claude（付費）** → 貼上 → 測試連線

> 把金鑰存在備忘錄，換手機時才不用重新申請。
> 金鑰只存在你手機的 Safari 裡，App 直接連該服務，不經過任何第三方伺服器。

### 金鑰能解鎖的六個功能

1. **同義句判斷 ＋ 其他說法** — 見上面第二之二節，這是最常用到的
2. **單句點評** — 練完某句按「✨ AI 點評」，告訴你哪個字發音錯、母語者更常怎麼講
3. **情境對話** — 咖啡廳、飯店櫃檯、機場入境等 10 個情境，AI 扮演對方跟你一來一往，**每一輪當場**糾正你剛剛那句
4. **自由聊天 ＋ 檢討報告** — 想聊什麼都行，聊天中 AI 完全不打斷你。按「結束並產生檢討報告」後會給你：
   - 兩句話總評 ＋ CEFR 程度估計（A2、B1…）
   - **文法錯誤清單**：你說了什麼 → 該怎麼說 → 錯在哪
   - **反覆出現的壞習慣**：例如老是漏三單現、時態前後不一致、句子都太短
   - **建議練的句子**：一鍵「存進錯題本」，之後就能拿來當題目練
   - 報告會留存，「我的」頁可以回看過去 20 份，比較自己的進步
5. **AI 產生新題庫** — 輸入主題（例如「在日本搭電車」），AI 生成一批句子存進手機，離線也能練

### 費用

用 Gemini 免費層：**0 元**，只要不超過每天次數上限。

用 Anthropic：按用量付費沒有月租，單句判斷／每輪對話約 0.02～0.05 元台幣，一份完整檢討報告約 0.3 元，一個月天天練通常不到 50 元台幣。想更省可以在設定裡把模型切成 Haiku 4.5，價格約五分之一。

---

## 六、其他

- **題庫**：內建 750 句
  - 日常會話 360 句（寒暄、點餐、購物、問路、工作、科技、通勤、情緒、社交、居家、運動健康、金錢、學習、抱怨道歉、句型強化、天氣、家庭、興趣嗜好、稱讚鼓勵、借東西、派對聚會、網路社群、拒絕婉拒、提醒叮嚀、道別、求助幫忙、環保永續、育兒、寵物、理財投資、職涯發展、安慰打氣、意見衝突、時間管理、網路購物退換貨）
  - 旅遊英文 240 句（機場、飛機上、飯店、民宿、交通、觀光、訂位、換匯、當地體驗、戶外、寄送、緊急狀況、回程、租車自駕、導覽解說、網路訂房、拍照攝影、排隊等候、遺失物品、搭船渡輪、露營野餐、市集殺價、天氣災害應變、異地用餐、背包客棧交流、網路與通訊）
  - 單字強化 150 句（專為填空設計，日常句子＋有難度的單字）
  - 可以自己新增句子，也可以讓 AI 生成
- **AI 供應商**：「我的 → AI 設定」可隨時在 Gemini（免費）與 Claude（付費）之間切換，兩把金鑰各自保存
- **口音／語速**：「我的」頁可以切美式／英式／澳洲，語速可以調慢到 0.5 倍練聽力
- **手機電腦同步**：見第八節　·　**備份**：見第九節

---

## 七、更新程式（電腦改完，手機自動跟上）

改過任何檔案後：

1. 把 `sw.js` 裡的 `echo-v11` 往上加一號（例如 `echo-v12`）——**這步不做，手機不會知道有新版**
2. 整個資料夾重新拖到 Netlify（同一個站台會覆蓋，網址不變）
3. 手機打開 App，畫面下方會跳出藍色的「有新版本可以更新」→ 按一下就好

App 每次從背景切回前景都會檢查一次新版，另外每小時也會自己檢查。不用再手動把 App 滑掉重開了。

題庫也一樣：`bank.js` / `bank2.js` 加了句子，走上面流程手機就會拿到。**AI 生成的句子不走這條**，那是資料，會走下面的雲端同步。

---

## 八、雲端同步（手機和電腦共用一份進度）

在手機練完，電腦一開就看得到；電腦加的自訂句子，手機也會有。**兩邊各自練的都會保留，不是誰覆蓋誰。**

> AI 金鑰不會上傳，只留在各自的裝置上。第二台裝置要自己填一次金鑰。

### 設定（第一台，約 10 分鐘，只做一次）

App 裡「我的 → 雲端同步 → 設定」有完整步驟，摘要：

1. 開 **supabase.com** → Start your project → 用 Google 或 GitHub 登入 → **New project**
   名稱隨便取，資料庫密碼隨便設但記著，地區選 **Northeast Asia (Tokyo)** → Create，等約 2 分鐘
2. 左側 **SQL Editor** → New query → 貼上 App 裡給你的那段 SQL（有複製鈕）→ 按 **Run**，看到 Success
3. 拿網址與金鑰。最快是點專案畫面**最上方的 Connect 按鈕**，網址和金鑰都在裡面；或走左側 **Settings → API Keys**
   - **Project URL** 長得像 `https://abcdefgh.supabase.co`，中間那串是系統給你的專案代號，每個人都不一樣
   - **金鑰**用 **Publishable key**（`sb_publishable_` 開頭）；舊專案叫 **anon public**（`eyJ` 開頭），兩種都行
   - ⚠️ 不要用 **Secret** 或 **service_role** 那把，那是給伺服器用的
4. 回 App 貼上這兩欄，**同步代碼**按「產生」隨機生一組
5. 按 **儲存並測試連線**，看到「雲端同步已啟用 ✅」就好

### 設定（第二台，30 秒）

第一台按「📋 複製整組設定給另一台」，把那串 `ECHOSYNC1:` 傳到第二台（LINE 給自己最快），第二台在同一個地方按「📥 貼上另一台的設定」→ 套用。**兩台的同步代碼一定要一模一樣**，用這個方式就不會打錯。

### 什麼時候會同步

- 開啟 App 時拉一次
- 練完一回合自動上傳
- 切到背景／關掉 App 時上傳
- 從背景切回來超過 3 分鐘也會拉一次
- 隨時可以按「🔄 立即同步」

不想自動的話，「我的 → 雲端同步 → 自動同步」可以關掉。

### 費用與安全

Supabase 免費層對這種用途遠遠用不完（整份資料通常不到 1 MB）。**同步代碼等於密碼，別公開。** 資料只有英文練習進度，沒有金鑰、沒有個資。

### 萬一同步出錯

App 會在「雲端同步」標題右邊顯示紅點與原因。常見的兩種：

- **找不到資料表** → 第 2 步的 SQL 沒跑成功，回 SQL Editor 重跑一次
- **金鑰不對或權限不足** → 貼成 `service_role`／`Secret` 金鑰了，要用 **Publishable** 或 **anon public** 那一把
- **資料表結構不對** → SQL 只跑了一半，整段重貼再跑一次

同步壞掉時本機資料完全不受影響，照樣可以練。

---

## 九、備份（請認真看這節）

就算開了雲端同步，還是建議偶爾留一份備份碼。App 在每台裝置上同時寫進兩個地方（localStorage 和 IndexedDB），其中一個被系統清掉會自動從另一個救回來，開啟時會跳「已從備援儲存救回你的進度」。**但兩個一起被清就真的沒了**，會發生這種事的情況：

- 在 Safari 設定裡按「清除瀏覽資料」
- 把主畫面的 App 刪掉重裝
- 換手機、重置手機
- 手機儲存空間爆滿時，系統自行清理

### 怎麼備份（10 秒）

「我的 → 備份與資料 → **📋 複製備份碼**」，會複製一長串 `ECHO1:` 開頭的文字。貼到<b>備忘錄</b>、傳給自己的 LINE、或存到雲端筆記都行。

超過 7 天沒備份，App 開啟時會提醒你一次，「我的」頁也會出現紅色警告。

### 怎麼還原

新手機裝好 App 後 →「我的 → 備份與資料 → **📥 貼上備份碼還原**」→ 貼上那串 `ECHO1:` → 按還原。學習進度、錯題本、同義句、自訂題目、API 金鑰、所有設定都會完整回來。

也可以用「存成檔案／讀取檔案」走 JSON 檔，效果一樣，適合存到 iCloud 雲碟。

---

## 十、養成每天練的習慣

iPhone 的網頁 App 不能主動推播通知，但用內建的**「捷徑」**App 可以做到一樣的事，而且點下去直接開啟 Echo。App 裡「我的 → ⏰ 怎麼設每天提醒」有完整步驟，摘要如下：

1. 打開 **捷徑** App → 下方切到 **自動化** → 右上 **＋** → 選 **時間**
2. 挑一個固定有空的時間，重複選 **每天**
3. 選 **立即執行**，把「執行前先詢問」**關掉**
4. **下一步** → 搜尋動作 **「打開 App」** → 選 **Echo 英語** → 完成

睡前或通勤時間最容易養成習慣。想要只提醒不自動開啟，第 4 步改選「顯示通知」。

---

## 十一、檔案說明

| 檔案 | 用途 |
|---|---|
| `index.html` | App 本體（介面＋所有邏輯） |
| `bank.js` | 核心題庫 140 句 |
| `bank2.js` | 擴充題庫 210 句 |
| `bank3.js` | 擴充題庫 180 句（新增天氣、家庭、租車、遺失物品等情境） |
| `bank4.js` | 擴充題庫 220 句（新增道別、寵物、理財、露營等情境）。想手動加句子，照格式往這裡加最方便 |
| `sw.js` | 離線快取＋自動更新。改過任何檔案後，把裡面的版本號往上加一號 |
| `manifest.webmanifest` | App 名稱與圖示設定 |
| `icon-192.png` / `icon-512.png` | 主畫面圖示 |

---

## 常見狀況

**麥克風沒反應**
iOS 設定 → Safari → 麥克風 → 允許。若在主畫面 App 裡仍然不行，先用 Safari 開網址測一次，允許之後再回主畫面 App。真的不行就用打字模式，評分完全一樣。

**沒聲音**
iPhone 側邊的靜音開關要打開。第一次要先點過畫面任一處，iOS 才允許自動播放語音。

**辨識把我唸對的字聽錯**
語音辨識本來就會有誤差，尤其是短字和專有名詞。分數 85 以上就當作過了，不用追求 100。AI 點評時也會把辨識誤差考慮進去。

**AI 功能顯示「金鑰無效」**
Gemini：金鑰是 `AIza` 或 `AQ.` 開頭都正常，但必須是在 Google AI Studio 建的。
Anthropic：金鑰要 `sk-ant-` 開頭，而且帳號一定要有儲值，到 Console → Billing 加最低額度即可。

**Gemini 顯示「今天的免費額度用完了」**
把模型換成 Gemini 3.5 Flash-Lite，或等隔天重置。也可以在設定裡切到 Anthropic 應急。

**Gemini 顯示「this model is no longer available to new users」**
Google 停掉了舊模型。App 會自動換一個可用的，若沒換成功就按設定裡的「🔍 偵測哪些模型我能用」。

**手機沒跳出「有新版本」**
`sw.js` 裡的版本號沒改（要從 `echo-v9` 往上加），或還沒重新上傳。改完再把 App 切到背景再切回來就會檢查。

**Project URL 不知道要填什麼**
不是 Supabase 官網、也不是你瀏覽器上那串 `supabase.com/dashboard/project/…`。要填的是專案自己的 API 網址，長得像 `https://abcdefgh.supabase.co`，在專案畫面上方的 **Connect** 按鈕或 **Settings → API Keys** 裡面。忘記打 `https://` 或多帶了結尾斜線，App 會自動修正。

**兩台裝置的進度沒同步**
先確認兩台的**同步代碼一字不差**（用「複製整組設定」最保險），再按「🔄 立即同步」看有沒有紅色錯誤訊息。
