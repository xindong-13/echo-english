# Echo 英語 — 從零到能用，照著做就好

全部做完約 25 分鐘。**每一步做完再做下一步**，不要跳。

---

## 你手上有什麼

桌面上的 `Echo 英語` 資料夾，裡面 10 個檔案：

```
index.html              ← App 本體
bank.js                 ← 題庫 140 句
bank2.js                ← 題庫 210 句
bank3.js                ← 題庫 180 句（新增情境）
bank4.js                ← 題庫 220 句（新增情境）
sw.js                   ← 離線與自動更新
manifest.webmanifest    ← App 名稱與圖示設定
icon-192.png            ← 圖示
icon-512.png            ← 圖示
使用說明.md              ← 功能說明（這份不影響 App，可留可不留）
開始使用.md              ← 你正在看的這份
```

**上傳時 10 個檔案全部一起傳，一個都不能少。**（兩個 .md 傳不傳都行，不影響運作。）

---

# 第一階段：把 App 放到網路上（電腦操作，約 5 分鐘）

## 步驟 1｜打開 Netlify Drop

電腦瀏覽器開：**https://app.netlify.com/drop**

## 步驟 2｜把資料夾拖進去

把桌面上整個 **`Echo 英語` 資料夾**，直接用滑鼠拖到那個網頁的虛線框裡放開。

> ⚠️ 是**拖整個資料夾**，不是先打開資料夾再拖裡面的檔案。

等幾秒鐘，畫面會開始跑上傳進度。

## 步驟 3｜拿到你的網址

上傳完會出現一個網址，長得像：

```
https://random-name-123abc.netlify.app
```

**這串網址是 Netlify 隨機給你的，我沒辦法事先知道，也給不了你。**
上傳完才會生出來，請自己複製起來。

先在電腦上點開它確認能用——應該會看到黑底綠字的 Echo 英語畫面。

## 步驟 4｜註冊帳號把網址固定下來

畫面上會有 **Sign up / Claim your site** 之類的提示，用 Google 帳號登入就好。

**不註冊的話這個網址過幾小時就會失效。**

登入後可以改網址名稱：`Site configuration` → `Change site name` → 改成好記的，例如 `jerry-echo`，網址就變成 `https://jerry-echo.netlify.app`。

## 步驟 5｜把網址存起來

把最終網址記到備忘錄，等一下手機要用。

---

# 第二階段：裝到 iPhone（手機操作，約 2 分鐘）

## 步驟 6｜用 Safari 打開網址

iPhone 上開 **Safari**（一定要 Safari，Chrome 在 iOS 不支援這個功能），輸入剛剛那個網址。

## 步驟 7｜加到主畫面

1. 點畫面**最下方中間的「分享」圖示**（方框加向上箭頭）
2. 往下滑，找到 **「加入主畫面」**
3. 名稱維持 `Echo 英語` → 右上角 **加入**

主畫面就會出現綠色麥克風圖示。**之後都從這個圖示打開**，會是全螢幕、沒有網址列。

## 步驟 8｜測試麥克風

打開 App → 直接按 **開始練習** → 點中間的綠色麥克風 → 會跳出詢問麥克風權限 → 選 **允許**。

隨便說一句英文，看有沒有跳出分數。

> 如果麥克風不能用：iOS 設定 → Safari → 麥克風 → 允許。
> 真的不行也沒關係，按 **⌨️ 用打的** 一樣能練，評分完全相同。

**做到這裡，App 就已經完全可以用了。**下面兩個階段是選配。

---

# 第三階段：開啟 AI 功能（選配，免費，約 3 分鐘）

不做這步也能用：發音評分、間隔複習、錯題本、350 句題庫、四種模式全部正常。
做了才有：同義句判斷、AI 點評、情境對話、自由聊天檢討、AI 生題。

## 步驟 9｜拿 Google 免費金鑰

手機或電腦開：**https://aistudio.google.com/apikey**

用 Google 帳號登入 → 按 **Create API key** → 複製整串（`AIza` 或 `AQ.` 開頭都對）。

不用信用卡，不用付費。

## 步驟 10｜填進 App

手機 App →「**我的**」→ **AI 設定**：

1. 上面確認選的是 **Google Gemini（免費）**
2. 金鑰欄位貼上 → 鍵盤按 **完成**（會跳「已儲存金鑰」）
3. 按 **測試連線** → 出現「連線成功 ✅」

> 失敗的話按 **🔍 偵測哪些模型我能用**，它會自動挑一個可用的。

---

# 第四階段：手機電腦同步（選配，約 10 分鐘）

只有手機在用的話**可以跳過**。想在電腦也練、而且進度共用才需要。

## 步驟 11｜建 Supabase 免費專案

電腦開 **https://supabase.com** → **Start your project** → 用 Google 登入 → **New project**

- Name：隨便取，例如 `echo`
- Database Password：隨便設但**記下來**
- Region：選 **Northeast Asia (Tokyo)**

按 Create，等約 2 分鐘跑完。

## 步驟 12｜建資料表

在 App 裡（手機或電腦瀏覽器都行）→「我的」→ **雲端同步** → **設定** → 找到那段 SQL → 按 **📋 複製這段 SQL**。

回到 Supabase → 左側 **SQL Editor** → **New query** → 貼上 → 按 **Run**。

看到 **Success** 就對了。

## 步驟 13｜拿網址與金鑰

Supabase 專案畫面**最上方有一顆 `Connect` 按鈕**，點開就看得到；或走左側 **Settings → API Keys**。

要拿兩樣：

| 欄位 | 長什麼樣 |
|---|---|
| **Project URL** | `https://abcdefgh.supabase.co` |
| **金鑰** | `sb_publishable_...`（或舊版 `eyJ...`） |

> ⚠️ **不要**用 `Secret` 或 `service_role` 那把。

## 步驟 14｜填進 App 並啟用

回 App「我的 → 雲端同步 → 設定」：

1. 貼上 Project URL
2. 貼上金鑰
3. **同步代碼**按 **產生**（會隨機生一組）
4. 按 **儲存並測試連線**

看到「雲端同步已啟用 ✅」就完成了。

## 步驟 15｜設定第二台裝置

在**已設定好的那台**按 **📋 複製整組設定給另一台**，會複製一串 `ECHOSYNC1:` 開頭的文字。

用 LINE 傳給自己 → 在**另一台**打開同一個網址（電腦就直接開網址，手機就開主畫面圖示）→「我的 → 雲端同步 → 設定」→ **📥 貼上另一台的設定** → 套用。

之後兩台就會自動同步了。

> 第二台要**自己再填一次 AI 金鑰**（步驟 10），因為金鑰基於安全考量不會上傳。

---

# 題庫要加句子，要不要重傳？

看你用哪種方式加：

| 加句子的方式 | 要重傳嗎 | 說明 |
|---|---|---|
| App →「題庫」→ **✨ 用 AI 產生新題庫** | **不用** | 存在你的資料裡，走雲端同步，另一台自動就有 |
| App →「題庫」→ **＋ 自己新增句子** | **不用** | 同上 |
| 聊天檢討報告的 **存進錯題本** | **不用** | 同上 |
| 直接編輯 `bank2.js` 檔案 | **要** | 那是程式碼，得跟 App 一起重新上傳 |

**日常加題目用 App 內建的功能就好，完全不用碰 Netlify。**
只有要大批新增、或想永久寫進題庫檔案時，才走下面的重傳流程。

---

# 之後想改東西的話

改了電腦上資料夾裡的任何檔案（例如在 `bank2.js` 加句子）：

**1. 改版本號**

打開 `sw.js`，把第 2 行的 `echo-v11` 改成 `echo-v12`（下次再改就 v13，一直往上加）。
**這步不做，手機不會知道有新版。**

**2. 重新上傳到「原本那個站台」**

打開 **https://app.netlify.com** → 登入 → 在站台列表點你的站台（例如 `jerry-echo`）→ 上方分頁選 **Deploys** → 頁面下方有一塊虛線區寫著 *Drag and drop your site output folder here* → 把整個 `Echo 英語` 資料夾拖進去。

> ⚠️ **不要再用 app.netlify.com/drop。**
> 那個頁面每拖一次就會**開一個全新的站台、給你一個全新的網址**，你手機上裝的還是舊網址，永遠不會更新。
> 一定要進到原本站台的 **Deploys** 分頁去拖，網址才會維持不變、直接覆蓋。

**3. 手機更新**

打開 App，畫面下方會跳出藍色的「有新版本可以更新」→ 按一下。

沒跳出來的話，把 App 切到背景再切回來（它每次回前景都會檢查），或確認第 1 步的版本號真的改了。

---

# 開始練之後，記得做這件事

「我的 → 備份與資料 → **📋 複製備份碼**」，貼到手機備忘錄。

每週一次就好，10 秒的事。萬一手機資料被系統清掉，這串碼是唯一能救回全部進度的東西。

---

# 卡住的時候

| 狀況 | 怎麼辦 |
|---|---|
| 網頁一片空白 | 檔案沒傳齊，8 個要一起傳 |
| 找不到「加入主畫面」 | 不是用 Safari 開的 |
| 沒聲音 | iPhone 側邊靜音開關要打開，且要先點過畫面一次 |
| 麥克風沒反應 | iOS 設定 → Safari → 麥克風 → 允許；或改用打字模式 |
| AI 說金鑰無效 | Gemini 要在 aistudio.google.com 建的才行 |
| 同步失敗 | 看紅字訊息，多半是 SQL 沒跑或貼到 Secret 金鑰 |
| 手機沒跳更新提示 | `sw.js` 版本號沒改，或還沒重新上傳 |
