# Echo 英語 — 從 Netlify 搬到 GitHub Pages

跟你的 Kotoba 同一套做法。**GitHub Pages 沒有每月部署次數限制**，想更新幾次都行。

你的 `gh` 已經裝好、也登入過 `xindong-13` 了，所以會比第一次弄 Kotoba 快很多。

---

## ⚠️ 動手前先看這一段

### 1. 你的備份碼檔案不會被上傳（我已經擋掉了）

資料夾裡的 `備份.txt` 內容包含：

- 你的 Gemini API 金鑰
- 你的 Supabase 網址、金鑰、同步代碼

GitHub repo 是**公開**的，這種東西傳上去等於把鑰匙貼在網路上。`.gitignore` 已經設定成排除它，設定腳本裡也加了兩道檢查，發現有備份檔要被上傳會直接中止。

**但請你自己也養成習慣：備份碼放在手機備忘錄就好，不要放在這個資料夾裡。**

### 2. 換網址 = 手機認定是新 App，進度不會自己跟過去

網址從 `xxx.netlify.app` 變成 `xindong-13.github.io/echo-english`，對瀏覽器來說是**兩個完全不同的網站**，本機資料不共通。

所以搬家順序很重要，**照下面第二階段做，不要先刪舊的**。

---

# 第一階段：上傳到 GitHub（電腦，約 3 分鐘）

## 步驟 1

在 `Echo 英語` 資料夾裡，**按兩下 `GitHub-第一次設定.bat`**。

## 步驟 2

它會依序檢查：

| 檢查項目 | 你要做什麼 |
|---|---|
| `.gitignore` 安全檢查 | 不用做，自動 |
| git 有沒有裝 | 你弄 Kotoba 時裝過了，會直接過 |
| GitHub CLI 有沒有裝 | 同上 |
| GitHub 登入 | 應該顯示 `Already logged in` |
| **Repository name** | **這裡要打字**（見下一步） |

## 步驟 3｜取 repo 名稱

它會問 repository name，預設是 `echo-english`。

直接按 **Enter** 用預設就好，網址會是：

```
https://xindong-13.github.io/echo-english/
```

> 不要取跟 `kotoba` 一樣的名字，會蓋掉你的日文 App。

## 步驟 4｜等它跑完

它會自動：建立 repo → 上傳檔案 → 開啟 GitHub Pages → 把網址寫進 `github-url.txt` → 用瀏覽器打開。

**第一次建置要等 1～2 分鐘。** 開起來如果是 404，等一分鐘重新整理就好。

在電腦上確認能看到黑底綠字的 Echo 畫面，再往下做。

---

# 第二階段：手機搬過去（約 3 分鐘）

**順序很重要，一步一步來。**

## 步驟 5｜先從舊 App 拿備份碼

打開**舊的**（Netlify 那個）Echo 圖示 →「我的」→ **📋 複製備份碼**。

複製起來後，**傳一份到自己的 LINE 或貼到備忘錄**，不要只留在剪貼簿。

## 步驟 6｜用 Safari 開新網址

iPhone 上開 **Safari**，輸入：

```
https://xindong-13.github.io/echo-english/
```

## 步驟 7｜加到主畫面

分享圖示 → **加入主畫面** → 加入。

主畫面會出現第二個綠色麥克風圖示（先跟舊的並存）。

## 步驟 8｜還原進度

打開**新的**圖示 →「我的」→ **📥 貼上備份碼還原** → 貼上 → 還原。

## 步驟 9｜檢查東西都在（不要跳過）

在新 App 裡確認：

- [ ] 「我的」→ 連續天數、總練習次數跟舊的一樣
- [ ] 「我的」→ 錯題本數字對得上
- [ ] 「我的」→ AI 設定裡金鑰還在，按 **測試連線** 成功
- [ ] 「我的」→ 雲端同步顯示綠點，按 **🔄 立即同步** 成功
- [ ] 「題庫」→ 你自己加的、AI 生成的句子都在
- [ ] 隨便練一題，麥克風正常、有分數
- [ ] 「我的」最下面顯示 `版本 echo-v11`

## 步驟 10｜刪掉舊的

上面全部打勾之後，**長按舊的圖示 → 移除 App → 從主畫面移除**。

> 舊的 Netlify 站台可以留著不管，也可以到 Netlify 後台刪掉，都不影響。

---

# 之後怎麼更新

改完任何檔案（例如在 `bank2.js` 加句子）：

**按兩下 `GitHub-一鍵更新.bat`** — 就這樣。

它會自動：

1. 把 `sw.js` 的版本號 +1（`echo-v11` → `echo-v12`）
2. 同時把 `index.html` 裡顯示的版本號改成一樣
3. commit 並 push 到 GitHub
4. GitHub 約 1 分鐘後重新建置

然後手機打開 App，下方跳出藍色「有新版本可以更新」→ 按一下。

**確認更新成功**：「我的」頁最下面的版本號有沒有跟著變。

> 跟 Netlify 不同，**版本號不用自己手動改了**，腳本會處理。

---

# 檔案分工

| 檔案 | 什麼時候用 |
|---|---|
| `GitHub-第一次設定.bat` | 只跑一次，搬家用 |
| `GitHub-一鍵更新.bat` | **之後每次更新都用這個** |
| `GitHub-用權杖登入.bat` | 只有瀏覽器登入一直失敗（503）時才用 |
| `bump-version.ps1` | 不用管，更新腳本自己會呼叫 |
| `.gitignore` | 不用管，保護你的備份檔不外流 |
| `.nojekyll` | 不用管，讓 GitHub 直接照原樣提供檔案 |
| `github-url.txt` | 跑完設定後會自動生成，裡面是你的新網址 |

---

# 卡住的時候

**登入一直 503**
先跑 `GitHub-用權杖登入.bat`，再跑第一次設定。

**網址開起來 404**
第一次建置要 1～2 分鐘，等一下重新整理。超過 5 分鐘還不行，去 GitHub repo 的 Settings → Pages 看 Source 是不是 `main` / `/ (root)`。

**畫面空白、沒有樣式**
`.nojekyll` 不見了。重跑一次更新腳本會自動補回來。

**更新後手機沒跳提示**
把 App 完全滑掉再開。還是沒有的話，去看「我的」頁的版本號是不是已經變了——變了就是其實已經更新成功。

**推送失敗**
在資料夾裡開命令提示字元跑 `gh auth login` 重新登入。

---

# 為什麼要搬

| | Netlify 免費版 | GitHub Pages |
|---|---|---|
| 每月部署次數 | 有上限 | **無限** |
| 每月流量 | 100 GB | 100 GB |
| 更新方式 | 手動拖資料夾 | 按一個 .bat |
| 版本號 | 要自己改 | 腳本自動 +1 |
| 改壞了想回頭 | 沒有紀錄 | git 有完整歷史 |

對你這種會頻繁調整題庫的用法，GitHub Pages 明顯比較適合。
